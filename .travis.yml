language: go

go:
  - "1.10"
 
go_import_path: github.com/Unleash/unleash-client-go

before_install:
  - echo pwd
  - pushd pwd
  - mkdir Unleash
  - mv xcoulon/unleash-client-go Unleash
  - popd

script:
 - go get -t -v ./... 
 - go vet ./...
 - go test ./...